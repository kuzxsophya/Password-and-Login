from tkinter import
from tkinter import messagebox
import pickle


def registration()
    Label_error = None


frame = Frame(root, bd=10
frame.place(relx=0.15, rely=0.2, relwidth=0.7, relheight=0.6)

Label = Label(frame, text='Sing up', font='16'
Label.place(relwidht=1, relheight=0.1)

Label_login = Label(frame, text="Login:")
# Label_logi.place(relx=0.1, rely=0.2)
Label_login.place(rely=0.2, relwigth=0.35, relweidth=0.1)


def Login_form():
    print('Login_form')
    pass
